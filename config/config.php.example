<?php
// config/config.php
// This file is auto-generated by the installer. Do not edit manually.

// --- Application Settings ---

// Set the application's primary domain name.
// This is crucial for security features like Passkeys (WebAuthn).
// Example: 'example.com' (do not include 'www' or 'https://')
define('APP_DOMAIN', '{{APP_DOMAIN}}');

// --- Database Connection Settings ---

// Database host (e.g., 'localhost' or an IP address)
define('DB_HOST', '{{DB_HOST}}');

// Your database name
define('DB_NAME', '{{DB_NAME}}');

// Your database username
define('DB_USER', '{{DB_USER}}');

// Your database password
define('DB_PASS', '{{DB_PASS}}');

// --- Email Settings (Optional) ---
// Configure these if you want the application to send emails (e.g., for password resets).
// define('SMTP_HOST', 'smtp.example.com');
// define('SMTP_USER', 'your_email@example.com');
// define('SMTP_PASS', 'your_email_password');
// define('SMTP_PORT', 587); // Or 465 for SSL
// define('SMTP_FROM_EMAIL', 'no-reply@example.com');
// define('SMTP_FROM_NAME', 'Promptash');

// --- Security Settings ---
// A long, random string used for security purposes (e.g., CSRF tokens).
// The installer will generate this for you.
define('SECRET_KEY', '{{SECRET_KEY}}');

// --- Other Settings ---

// Set the default timezone for the application
date_default_timezone_set('UTC');

// Enable or disable debug mode (true for development, false for production)
define('DEBUG_MODE', false);

if (DEBUG_MODE) {
    ini_set('display_errors', 1);
    ini_set('display_startup_errors', 1);
    error_reporting(E_ALL);
} else {
    ini_set('display_errors', 0);
    ini_set('display_startup_errors', 0);
    error_reporting(0);
}
?>
